{
  "name": "halolink",
  "version": "0.0.1",
  "private": true,
  "description": "HaloLink — API-first scheduling + contact-form platform",
  "packageManager": "pnpm@11.6.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "build": "pnpm --filter \"./apps/*\" build",
    "lint": "pnpm -r --if-present lint",
    "web": "pnpm --filter @halolink/web",
    "docs": "pnpm --filter @halolink/docs",
    "sdk": "pnpm --filter @halolink/sdk",
    "proto": "buf generate",
    "proto:lint": "buf lint"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
